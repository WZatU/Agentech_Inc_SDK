# L1.0 SDK Cards

Narrow deterministic primitives with typed input/output, bounded side effects,
and primitive-level validation or evidence.

Card path: `cards/<robot>/<En|Ch>/<category>/<card>.md`.

Unreviewed existing cards: `cards/Unclassify/<En|Ch>/<card>.md`.
