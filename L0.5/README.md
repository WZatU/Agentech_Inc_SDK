# L0.5 SDK Cards

Direct bounded SDK actions, state reads, configuration calls, image operations,
transform lookups, and adapter calls.

Card path: `cards/<robot>/<En|Ch>/<category>/<card>.md`.

Unreviewed existing cards: `cards/Unclassify/<En|Ch>/<card>.md`.
